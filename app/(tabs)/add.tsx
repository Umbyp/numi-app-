// Dummy route — the tab bar intercepts presses on this tab and opens the quick-add
// bottom sheet instead of ever navigating here (see app/(tabs)/_layout.tsx).
export default function AddPlaceholder() {
  return null;
}
